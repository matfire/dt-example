{% extends "base.html.twig" %}

{# {% block styles %}
	{{ encore_entry_link_tags('datatable') }}
{% endblock %} #}

{% block body %}
    <div {{ stimulus_controller('datatable', {'buttons': ['csv', 'copy']})}}>
	<table data-datatable-target="table">
		<thead>
			<tr>
				<th>
					Box
				</th>
				<th>
					Date Derniere Transmission
				</th>
				<th>
					Etat
				</th>
				<th>
					Actions
				</th>
			</tr>
		</thead>
		<tbody>
			{% for box in boxes %}
				<tr>
					<td>
						{{box.getBOXID()}}
					</td>
					<td>
						{{box.getBOXLastComDate()|date}}
					</td>
					{% if currentTs - box.getBOXLastComDate() > factor *  box.getBOXTransmissionSampleRate() %}
						<td class=" bg-red-300">
							Hors Ligne
						</td>
					{% else %}
						<td class=" bg-green-300">
							En Ligne
						</td>
					{% endif %}
					<td align="right">
						<a class="focus:outline-none text-white bg-purple-700 hover:bg-purple-800 focus:ring-4 focus:ring-purple-300 font-medium rounded-lg text-sm px-5 py-2.5 mb-2 dark:bg-purple-600 dark:hover:bg-purple-700 dark:focus:ring-purple-900" href="/resources/box/{{box.getBOXID()}}">Détails</a>
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
    </div>
{% endblock %}

{# {% block scripts %}
	{{ encore_entry_script_tags('datatable') }}
{% endblock %}
 #}